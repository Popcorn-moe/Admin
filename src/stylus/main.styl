@import './theme'
@import '../../node_modules/vuetify/src/stylus/main'
@import './transitions'

.no-margin {
  margin: 0;
}

.application--dark {
  h1, h2, h3, h4, h5, h6 {
    color: #D3D3D3;
  }
}

a, .list__tile--active {
  color: inherit;
}